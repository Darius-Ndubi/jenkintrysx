pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        sh "echo 'This will be stage to build the api'"
      }
    }

    stage('Run Tests') {
      steps {
        sh "echo 'This will be stage to build the api'"
      }
    }

    stage('upload_cover') {
      steps {
        sh "echo 'This will be stage to build the api'"
      }
    }
  }
}

