pipeline {
  agent any

  stages {
    stage('Build') {
      sh "This will be stage to build the api"
    }

    stage('Run Tests') {
      sh "This will be stage to build the api"
    }

    stage('upload_cover') {
      sh "This will be stage to build the api"
    }
  }
}
